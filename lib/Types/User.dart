//Creating a class user to store the data;
class User {
  final int id;
  final String title;
  final String email;
  
  User({
    required this.id,
    required this.title,
    required this.email,
  });
}